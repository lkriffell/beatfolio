<%= form_for :beat do |form| %>
    title: <%= form.text_field :name %>
    image: <%= form.text_field :image %>
    tags: (seperate with a comma) <%= form.text_field :tags %>
    <%= form.submit %>
<% end %>