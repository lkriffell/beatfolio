<%= form_for :beat do |form| %>
    title: <%= form.text_field :name %><br>
    Upload: <%= form.file_field :audio_file %><br>
    description: <%= form.text_area :description %><br>
    tags: (seperate with a comma) <%= form.text_field :tags %><br>
    <%= form.submit %>
<% end %>